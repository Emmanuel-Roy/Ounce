# Build directories
build/
**/build/
build_pico1/
.bin/

# Temporary / OS / Python files
__pycache__/
*.pyc
.VSCode/
.idea/
*.user
*.suo

# VS Code local history, previously committed here by accident
.history/

# Captured video and input logs, written beside the client
recordings/

# bin/OunceClient/ is committed deliberately - the point of bin/ is that you can
# clone and run without a toolchain. It is ~149MB across 181 files, so avoid
# rebuilding it in commits that do not need to change it.
