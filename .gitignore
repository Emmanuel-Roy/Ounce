# Build directories
build/
**/build/
build_pico1/
.bin/

# Temporary / OS / Python files
__pycache__/
*.pyc
.VSCode/
.idea/
*.user
*.suo

# VS Code local history, previously committed here by accident
.history/

# Standalone client build (~149MB, reproducible via Ounce-Client/build_exe.bat).
# The prebuilt exe lives in bin/OunceClient/ on disk but is kept out of git:
# it is 181 files including an 84MB ffmpeg binary, and a fresh copy of all of
# it would land in history on every rebuild.
bin/OunceClient/
